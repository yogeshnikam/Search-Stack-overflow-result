import React from 'react';

const Banner = (props) => (
    <div>
    	<div className="page-header text-center banner">
    	  	<h1 className="text-muted font-weight-bold text-danger">
    	  		<em>Get Stackoverflow search</em>
      		</h1>
    	</div>
    </div>
);

export default Banner