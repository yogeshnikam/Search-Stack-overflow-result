import React from 'react';

const Home = (props) => (
    <div>
    	<h2><em> Hi and welcome to My StackOverflow !! Let's do some searching. Click search from the nav bar to navigate to the search page !!</em></h2>
    </div>
);

export default Home